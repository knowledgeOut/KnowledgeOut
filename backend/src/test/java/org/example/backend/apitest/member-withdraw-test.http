### 0. 회원가입 (테스트 계정 생성)
POST http://localhost:8080/api/knowledgeout/members/signup
Content-Type: application/json

{
  "email": "test@test",
  "password": "qwer1234",
  "nickname": "테스트"
}

### 1. 로그인 (세션 생성)
POST http://localhost:8080/api/knowledgeout/members/login
Content-Type: application/json

{
  "email": "test@test",
  "password": "qwer1234"
}

> {%
    // 로그인 성공 시 쿠키가 자동으로 저장됩니다.
%}


### 2. 현재 내 정보 조회 (탈퇴 전)
GET http://localhost:8080/api/knowledgeout/members/mypage
Accept: application/json

> {%
    client.test("상태 확인", function() {
        client.assert(response.status === 200, "조회 성공");
        client.assert(response.body.status === "ACTIVE", "현재 상태 ACTIVE 확인");
    });
%}


### 3. 회원 탈퇴 실행
DELETE http://localhost:8080/api/knowledgeout/members/mypage/withdraw

> {%
    client.test("탈퇴 처리 확인", function() {
        client.assert(response.status === 204, "정상 탈퇴 완료");
    });
%}


### 4. 탈퇴 후 조회 시도 (실패 케이스)
# 탈퇴 시 이메일이 null이 되어 세션의 Principal 정보를 찾을 수 없게 됩니다.
GET http://localhost:8080/api/knowledgeout/members/mypage

> {%
    client.test("탈퇴 후 접근 차단 확인", function() {
        client.assert(response.status === 400 || response.status === 403, "접근 거부 확인");
    });
%}